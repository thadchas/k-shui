apiVersion: v1
kind: Service
metadata:
  name: {{ include "k-shui.fullname" . }}
  labels:
    {{- include "k-shui.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k-shui.selectorLabels" . | nindent 4 }}
