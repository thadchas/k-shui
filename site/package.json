{
  "name": "k-shui-website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare:content": "node scripts/prepare-content.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "npm run prepare:content && astro dev",
    "build": "npm test && npm run prepare:content && astro build && node scripts/check-links.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "7.3.1",
    "@astrojs/starlight": "0.42.0",
    "astro": "7.3.2",
    "mermaid": "11.17.2"
  },
  "devDependencies": {
    "parse5": "8.0.1"
  }
}
