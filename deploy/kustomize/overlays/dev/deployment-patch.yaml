apiVersion: apps/v1
kind: Deployment
metadata:
  name: k-shui
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: k-shui
          imagePullPolicy: Always
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
